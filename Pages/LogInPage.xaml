<Page x:Class="pr15.Pages.LogInPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pr15.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Вход">

    <Grid>
        
        <StackPanel>
            <Label FontSize="16">Страница входа</Label>

            <StackPanel>
                <Label>Пароль</Label>
                <PasswordBox x:Name="password" >
                   
                </PasswordBox>
                <ItemsControl  Grid.Row="4" 
FontSize="12"
ItemsSource="{Binding ElementName=password, Path=
(Validation.Errors)}">
                    <ItemsControl.ItemTemplate >
                        <DataTemplate>
                            <TextBlock Text="{Binding ErrorContent}" TextWrapping="Wrap"  />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <StackPanel>
                <Button Click="Button_Click">Войти как менеджер</Button>
                <Button Click="Button_Click_1" >Войти как посетитель</Button>
                
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
