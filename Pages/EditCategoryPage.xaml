<Page x:Class="pr15.Pages.EditCategoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pr15.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Редактор категории">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <StackPanel>
                <Label FontSize="16">Страница редактирования категорий</Label>
                <Label>Название категории</Label>
                <TextBox x:Name="t1" >
                    <Binding Path="Name"  UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:IsTitleCorrect2/>
                        </Binding.ValidationRules>
                    </Binding>


                </TextBox>
                <Label></Label>

            </StackPanel>
            <StackPanel>
                <Button Click="Button_Click">Сохранить</Button>
                <Button Click="Button_Click_1">Назад</Button>
            </StackPanel>
        </StackPanel>

    </Grid>
</Page>
